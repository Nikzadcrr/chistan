name: C7 one secret
on:
  workflow_dispatch:
jobs:
  c7:
    runs-on: ubuntu-latest
    steps:
      - name: one secret
        env:
          MY_SECRET: ${{ secrets.KEYSTORE_PASSWORD }}
        run: echo "len=${#MY_SECRET}"
