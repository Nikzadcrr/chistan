name: C3 release action
on:
  workflow_dispatch:
jobs:
  c3:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
