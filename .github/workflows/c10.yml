name: C10 base64 secret only
on:
  workflow_dispatch:
jobs:
  c10:
    runs-on: ubuntu-latest
    steps:
      - name: big secret
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: echo "len=${#KEYSTORE_BASE64}"
